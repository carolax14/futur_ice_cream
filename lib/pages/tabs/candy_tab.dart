import 'package:flutter/material.dart';

class CandyTab extends StatelessWidget {
  const CandyTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("CANDY TAB"),
      ),
    );
  }
}
