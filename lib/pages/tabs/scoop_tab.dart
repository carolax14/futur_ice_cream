import 'package:flutter/material.dart';

class ScoopTab extends StatelessWidget {
  const ScoopTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("SCOOP TAB"),
      ),
    );
  }
}
