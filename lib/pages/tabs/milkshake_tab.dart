import 'package:flutter/material.dart';

class MilkshakeTab extends StatelessWidget {
  const MilkshakeTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("MILKSHAKE TAB"),
      ),
    );
  }
}
